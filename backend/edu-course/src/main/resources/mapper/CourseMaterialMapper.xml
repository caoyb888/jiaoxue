<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.smu.edu.course.repository.CourseMaterialMapper">

    <select id="selectMaterialPage" resultType="cn.smu.edu.course.domain.entity.CourseMaterial">
        SELECT id, teacher_id, title, file_type, original_path, slide_dir,
               page_count, file_size_kb, status, is_deleted, created_at, updated_at
        FROM course_material
        WHERE is_deleted = 0
          AND teacher_id = #{teacherId}
          <if test="keyword != null and keyword != ''">
              AND title LIKE CONCAT('%', #{keyword}, '%')
          </if>
        ORDER BY created_at DESC
    </select>

    <update id="updateConvertResult">
        UPDATE course_material
        SET slide_dir  = #{slideDir},
            page_count = #{pageCount},
            status     = #{status},
            updated_at = NOW()
        WHERE id = #{materialId}
    </update>

</mapper>
