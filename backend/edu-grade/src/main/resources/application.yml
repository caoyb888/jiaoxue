server:
  port: 8086

spring:
  application:
    name: edu-grade
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:100.84.68.115:18848}
        namespace: ${NACOS_NAMESPACE:dev}
      config:
        server-addr: ${NACOS_ADDR:100.84.68.115:18848}
        namespace: ${NACOS_NAMESPACE:dev}
        file-extension: yaml
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://100.84.68.115:13306/edu_db?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: edu_dev_2026
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
  flyway:
    enabled: false
  data:
    redis:
      host: 100.84.68.115
      port: 16379
      database: 4

xxl:
  job:
    admin:
      addresses: ${XXL_JOB_ADMIN:http://100.84.68.115:18160/xxl-job-admin}
    access-token: ${XXL_JOB_TOKEN:edu-xxljob-token-2026}
    executor:
      appname: edu-grade
      address:
      ip: ${XXL_JOB_EXECUTOR_IP:}
      port: ${XXL_JOB_EXECUTOR_PORT:9987}
      logpath: ${XXL_JOB_LOGPATH:/tmp/edu-xxl-job/jobhandler}
      logretentiondays: 7

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: isDeleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    cn.smu.edu: INFO
