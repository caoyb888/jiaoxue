<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.smu.edu.jwxt.repository.JwxtRawDataMapper">

    <insert id="batchInsert">
        INSERT INTO jwxt_raw_data (sync_log_id, data_type, jwxt_id, raw_json, status, error_msg, created_at)
        VALUES
        <foreach collection="list" item="r" separator=",">
            (#{r.syncLogId}, #{r.dataType}, #{r.jwxtId}, #{r.rawJson},
             #{r.status}, #{r.errorMsg}, NOW())
        </foreach>
    </insert>

    <select id="selectPendingByType" resultType="cn.smu.edu.jwxt.domain.entity.JwxtRawData">
        SELECT id, sync_log_id, data_type, jwxt_id, raw_json, status, error_msg, created_at
        FROM jwxt_raw_data
        WHERE data_type = #{dataType}
          AND status = 0
        ORDER BY id
        LIMIT #{limit}
    </select>

    <update id="updateStatusByIds">
        UPDATE jwxt_raw_data
        SET status = #{status},
            error_msg = #{errorMsg}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
