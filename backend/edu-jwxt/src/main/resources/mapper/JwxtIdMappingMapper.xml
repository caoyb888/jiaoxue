<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.smu.edu.jwxt.repository.JwxtIdMappingMapper">

    <select id="selectLocalIdByJwxtId" resultType="java.lang.Long">
        SELECT local_id
        FROM jwxt_id_mapping
        WHERE data_type = #{dataType}
          AND jwxt_id = #{jwxtId}
        LIMIT 1
    </select>

    <select id="selectJwxtIdByLocalId" resultType="java.lang.String">
        SELECT jwxt_id
        FROM jwxt_id_mapping
        WHERE data_type = #{dataType}
          AND local_id = #{localId}
        LIMIT 1
    </select>

    <insert id="batchUpsert">
        INSERT INTO jwxt_id_mapping (data_type, jwxt_id, local_id, sync_log_id, created_at, updated_at)
        VALUES
        <foreach collection="list" item="m" separator=",">
            (#{m.dataType}, #{m.jwxtId}, #{m.localId}, #{m.syncLogId}, NOW(), NOW())
        </foreach>
        ON DUPLICATE KEY UPDATE
            local_id    = VALUES(local_id),
            sync_log_id = VALUES(sync_log_id),
            updated_at  = NOW()
    </insert>

</mapper>
