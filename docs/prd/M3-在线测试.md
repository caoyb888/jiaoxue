# M3 在线测试（edu-exam / edu-grade）

> 对应 Sprint 4（题库与试卷）+ Sprint 5（在线考试与监考）。
> **本章是补全优先级最高的章节**：S5 四个页面已开发但前端不可达（见 00-差距盘点 🟠）。
> 填写时套用 README 的「单功能 PRD 模板」。

## 章节范围与功能清单

### 3.1 题库管理（S4，✅ 可达）
- F3-01 题库 CRUD / 私有·院系共享权限
- F3-02 题目 CRUD（6 种题型：单选/多选/判断/填空/简答/…）
- F3-03 富文本题干编辑器（Tiptap + 图片上传）
- F3-04 docx 批量导入（解析结果 + 错误行提示）
- F3-05 题目全文检索（ES）

### 3.2 试卷制作（S4，✅ 可达）
- F3-06 试卷 CRUD / 拖拽排序 / 分值配置 / ABC 卷 / 大题分组
- F3-07 试卷预览

### 3.3 课堂随堂作答（S4，✅ 可达）
- F3-08 课堂发题 → 学生端即时作答 → 客观题即时批改
- F3-09 ⚠️ **边界待厘清**：随堂作答(`StudentAnswerPage`) vs 正式考试作答(`ExamAnswerPage`) 的职责划分

### 3.4 正式考试全流程（S5，🟠 已开发·前端不可达，优先补全）
- F3-10 考试发布配置（时间/时长/密码/监考/人脸核验/乱序）— ⚠️ 发布页存疑
- F3-11 进入考试：密码验证 + 人脸核验 + 分批下题（`ExamEnterPage`，需接路由）
- F3-12 答题页容灾：IndexedDB 15s 草稿 + 断网恢复（C2，`useExamAutoSave`）
- F3-13 自动交卷打散：学号取模 0~30s（C2，`useAutoSubmit`）
- F3-14 防作弊：全屏水印 + 切屏监听 + 复制拦截 + 事件上报（`ExamWatermark`/`useExamMonitor`）
- F3-15 交卷容灾链路前端对接：幂等提交 + 提交态反馈

### 3.5 监考与阅卷（S5，🟠 已开发·前端不可达）
- F3-16 监考状态大屏（`MonitorDashboardPage`，需接路由）
- F3-17 教师阅卷（主观题评分/批注，`ReviewPage`，需接路由）

### 3.6 成绩（edu-grade，S4/S8 部分）
- F3-18 评分规则配置（权重合计=100 校验）

## 待补全 PRD 条目（按模板逐条展开）

> 优先级：先写 **3.4 / 3.5（S5 孤儿页）**，因为接通路由后立刻可用、见效最大。

<!-- 在此按 README 模板逐条填写 F3-10 ~ F3-17 -->

## 本章关联红线
- **C2** 交卷容灾（IndexedDB 草稿 + 取模打散 + 幂等 + Redis 暂存 + Kafka 落库）
- **C6** 人脸核验仅存比对结果，禁止存原始照片
- **C8** 小程序答题页 DOM API 隔离

## 已知差距（来自 00-差距盘点；2026-06-20 多轮更新）
| 条目 | 状态 | 待办 |
|---|---|---|
| ExamEnterPage / ExamAnswerPage / ReviewPage / MonitorDashboardPage | ✅ **已接路由** | `/exam/:publishId/enter`、`/exam/:publishId/answer`、`/exam/:publishId/review/:studentId`、`/exam/monitor?publishId=` |
| **考试发布列表页 ExamListPage** | ✅ **已补建** | `/exam/list`，教师/学生双视角；Dashboard「在线考试」入口指向此页。教师→进监考，学生选班级→进入考试 |
| **人脸核验页 FaceVerifyPage** | ✅ **已补建** | `/exam/:publishId/face-verify`，摄像头采集→比对→通过跳答题页。C6：照片只传不存 |
| **api 解包 bug（S5 全页）** | ✅ **已修** | 拦截器已解包 Result→data，但 enter/face/getQuestions/submit/heartbeat 及 Monitor/Review 页误读 `.data`（运行期取到 undefined）。已统一改为直返业务数据 |
| StudentAnswerPage vs ExamAnswerPage | ✅ **边界已厘清** | 前者=课堂随堂题（lesson 维度）；后者=正式考试（publish 维度），并存非重复 |
| **考试发布配置页 PublishConfigPage** | ✅ **已补建** | `/exam/publish`，确认 ExamPaperPage 此前无任何发布功能。新页：选班级+试卷+时间窗口/时长/密码/监考/人脸/复制/乱序 → `POST /publishes`。入口：ExamListPage「发布考试」+ ExamPaperPage 试卷详情「发布考试」（带 paperId/classId 预填） |
| **考试结果页 ExamResultPage** | ✅ **已补建** | `/exam/:publishId/result`，对接 `GET /publishes/{id}/my-score`：得分总览 + 客观题答对数 + 待批改提示 + 逐题明细。交卷后不再白屏 |
| **教师阅卷的学生入口** | ✅ **已接通** | 监考大屏每个已交卷（SUBMITTED）学生行加「阅卷」链接 → `/exam/:publishId/review/:studentId`；未交卷显示「未交卷」。ReviewPage 取数/PUT 批改路径均与后端核对一致 |
