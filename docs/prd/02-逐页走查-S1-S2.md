# 逐页功能走查 · Sprint 1–2

> 方式：**代码级走查**——核查每页 ①路由可达性 ②API 写法/端点正确性 ③PRD/Sprint AC 覆盖 ④bug 与缺口。
> 行为级验证（渲染、交互、三端响应式）标注为 🔵，需运行态点查（系统在内网机，可后续 /verify 跑）。
> 走查日期：2026-06-20　范围：S1（登录/用户/组织）+ S2（课程/课堂/课件）

## 结论分布图例
✅ 代码核查通过　🔵 需运行态确认　🟡 功能缺口（对 PRD）　🔴 Bug/断链（功能性损坏）

---

## S1 · M0 登录页 `LoginPage` → `/login`

| 项 | 状态 | 依据 / 说明 |
|---|---|---|
| 手机号 + 验证码登录 | ✅ | `authApi.sendSmsCode` + `loginByPhone`；登录后解码 JWT(base64url 规范化)取 userId → `getUserById` 补 profile → 落 store |
| 60s 倒计时 / 手机号正则 / 错误提示 | ✅ | `^1[3-9]\d{9}$`，登录失败提示"验证码错误或已过期" |
| 微信扫码登录 | 🟡 | **纯占位**：第 167-180 行是静态虚线框 +"（开发环境暂不可用）"，无二维码、无 callback 调用。S1-12/S1 AC 的微信链路前端未接 |
| JWT 存储合规 | 🟡 | 存于 **sessionStorage**(`edu_at`/`edu_rt`，见 client.ts)。满足"不落 localStorage"，但未达 S1-12 期望的"HttpOnly Cookie / 内存"；等保三级下仍有 XSS 读取面，建议评估 |
| 三端响应式（375/768/1280） | 🔵 | `max-w-sm` 卡片居中，需截图核 |

**dev 提示**：CLAUDE.md 注明 dev 验证码固定 123456、直接输入即可（勿点发送）。UI 支持直接输码登录，符合。

---

## S1 · M5/M0 用户管理 `UserManagePage` → `/admin/users`

| 项 | 状态 | 依据 / 说明 |
|---|---|---|
| 院系树侧栏（递归 DeptTreeItem） | ✅ | `userApi.getDeptTree`，点击过滤用户 |
| 用户列表 + 搜索（姓名/用户名/手机号）+ 类型筛选 + 分页 | ✅ | `getUsers({keyword,userType,deptId,pageNum})` |
| 角色分配 | ✅ | 弹窗 → `assignRoles(userId, selected)`（S1-14 核心） |
| 用户状态启停 | ✅ | `updateStatus` |
| **用户新增 / 编辑 / 删除** | 🟡 | 表格操作列仅"分配角色"，**无增/改/删 UI**。S1-07 列了 sys_user CRUD（注：用户主要由 S7 教务 ETL 导入，手工 CRUD 或可延后，但 PRD 需明确取舍） |
| @DataScope 数据权限（防越权读其他院系） | 🔵 | 后端 AOP，需用不同院系账号做越权测试验证 |
| 三端：院系侧栏 `hidden md:flex` | 🔵 | 手机端隐藏侧栏，需确认手机端如何切院系 |

---

## S2 · M1 课程列表 `CourseListPage` → `/courses`

| 项 | 状态 | 依据 / 说明 |
|---|---|---|
| 课程列表 + 关键词/学期搜索 + 分页 + 卡片 | ✅ | `useCourseList` → `/v1/course/list` |
| 课程卡跳转 | 🔴 | `CourseCard` `Link to="/course/${id}/classes"`（第 106 行），但**路由表无 `/course/:id/classes`** → 点击 fallthrough 回 `/dashboard`。课程→教学班 drill-down 断链 |
| 教学班列表页（双视角 S2-02/S2-11） | 🟡 | **无独立教学班列表页**。实际"班级列表"由 `InteractionEntryPage`（互动入口）承担，但它挂在互动模块、非从课程进入；且 CourseList 未按教师/学生角色区分视角 |

> 建议：补一个 `/course/:id/classes` 教学班列表页（或把 CourseCard 指向已有的互动入口/班级视图），打通"课程→班级→课堂"主路径。

---

## S2 · M1 课堂页 `ClassroomPage` → `/course/:classId/classroom`

| 项 | 状态 | 依据 / 说明 |
|---|---|---|
| 可达性 | ✅ | 经 `InteractionEntryPage` 第 63 行 `to=/course/${cls.id}/classroom` 可达（非经课程页） |
| 开始 / 结束课堂 | ✅ | `useStartLesson` / `useEndLesson` |
| 课件翻页 + WebSocket 同步 | ✅ | WebSocket 连接 + `updateSlide`，`JSON.parse(e.data)` 收翻页事件（S2-12 / S3-05） |
| **C5：默认 SLIDE_ONLY、不开 WebRTC** | ✅ | 开始课堂硬编码 `liveMode:'SLIDE_ONLY'`（第 67 行），全页**无 RTCPeerConnection/RTMP** 代码，合规 |
| currentSlide 维护 | ✅ | `courseApi.updateSlide(lessonId, newSlide)` |
| 线上课 ONLINE_CLASS 模式 | 🟡 | 未实现（属 S8 直播范畴，S2 不要求，记此备忘） |

---

## S2 · M1 课件管理 `MaterialManagePage` → `/materials`

| 项 | 状态 | 依据 / 说明 |
|---|---|---|
| 预签名三步上传（申请→PUT MinIO→complete） | ✅ | `applyUpload` → `fetch(PUT presignedUrl)` → `completeUpload`（S2-04 / S2-14） |
| 上传进度 / 错误处理 | ✅ | `res.ok` 检查、HTTP 错误提示 |
| 转换状态展示（转换中/完成/页数） | ✅ | STATUS_LABEL（0 转换中…），`pageCount` 展示 |
| **转换状态自动轮询** | 🟡 | 页面层 `useMaterialList` **无 `refetchInterval`**，转换中→完成不会自动刷新，需手动刷新页面。S2-14 AC 要求"转换状态轮询"，待补（或确认 hook 内是否轮询） |
| 三端拖拽上传 | 🔵 | 需运行态核拖拽与移动端表现 |

---

## S1–S2 走查汇总

### 🔴 Bug / 断链（建议优先修）
1. **CourseListPage 课程卡死链** → `/course/:id/classes` 无对应路由，课程→教学班入口失效。

### 🟡 功能缺口（对 PRD/Sprint AC）
1. 微信扫码登录前端未实现（占位）。
2. 用户管理缺 新增/编辑/删除 UI（仅角色分配+状态）。
3. 教学班列表页（双视角）缺失，且与课程页未打通。
4. 课件转换状态无自动轮询。
5. JWT 存 sessionStorage，未达"HttpOnly/内存"理想（等保关注点）。

### 🔵 需运行态确认（留待 /verify 跑）
- 三端响应式（登录页 / 用户管理院系侧栏 / 课件拖拽）。
- @DataScope 数据权限越权测试。
- WebSocket 翻页多端同步延迟（S2-12 AC：≤200ms）。
- 课件上传后 5 分钟内出现 slide 序列（S2-04 AC）。

### ✅ 代码核查通过（主干）
手机号登录、用户列表/院系树/角色分配、课程列表、课堂开始结束+WebSocket翻页+**C5 合规**、课件预签名三步上传。

> 下一步：S3（互动教学：签到/弹幕/点名）+ S4（题库/试卷/答题）逐页走查；S4 已预扫出多处 `.data` 解包 bug（QuestionBank/StudentAnswer/ExamPaper/DocxImport 的 mutation onSuccess），待该轮详查并修。
